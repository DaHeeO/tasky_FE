import styles from './TaskComponent.module.css';

const TaskComponent = () => {
    return (
        <div className={styles.wrapper}>
            <p>아직 미정임 어떻게 구현할지 ...t</p>
        </div>
    );
};

export default TaskComponent;
